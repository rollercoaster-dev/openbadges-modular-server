# Server configuration
PORT=3000
HOST=0.0.0.0

# Database configuration
DB_TYPE=postgresql
DATABASE_URL=postgres://postgres:postgres@localhost:5432/openbadges

# API configuration
BASE_URL=http://localhost:3000

# Asset storage configuration
# Supported: local (default; future: s3, cloudinary)
ASSETS_PROVIDER=local
# Directory for local storage (default: ./uploads)
ASSETS_LOCAL_DIR=uploads
# Maximum file size in bytes (default: 5MB)
ASSETS_MAX_FILE_SIZE=5242880
