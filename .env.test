# Environment variables for E2E Testing

# API Configuration
API_BASE_URL=http://localhost:3000 # Adjust if your test server runs on a different port

# Default DB_TYPE for test runs (can be overridden by specific test scripts)
# DB_TYPE=sqlite 
# DB_TYPE=postgresql

# PostgreSQL Test Database (matches docker-compose.test.yml)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=openbadges_test
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# SQLite Test Database
SQLITE_PATH=./test/e2e/test_database.sqlite # Path relative to project root
# For SQLite, DATABASE_URL might be set to something like: sqlite:${SQLITE_PATH}
